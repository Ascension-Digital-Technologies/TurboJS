# Benchmarks

Authored benchmark programs live under `benchmarks/jit/`. Historical machine-readable and text output lives under `benchmarks/results/`; build-tree measurements should remain under `build/` until intentionally archived.
