# Benchmark results

This directory contains curated, reproducible benchmark snapshots intended for release comparison. Development-iteration dumps and milestone-specific result files are intentionally excluded from the repository.

Benchmark numbers are meaningful only with the workload source, engine revision, build configuration, operating system, processor, and run methodology recorded together.
