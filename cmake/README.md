# CMake modules

The root `CMakeLists.txt` composes focused modules from this directory. Build policy, products, tests, generators, benchmarks, installation, JIT source ownership, and engine subsystem ownership remain separate so no monolithic build file can regrow.
