# Applications

Product entry points live here. `turbojs/` owns the standalone shell and its JavaScript bootstrap sources, `turbojsc/` owns bytecode compilation, and `wasi-reactor/` owns the WASI reactor entry point. Engine implementation does not belong in this tree.
